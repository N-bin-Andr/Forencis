'поиск номера заключени€ и заполнение данными €чеек таблицы
Dim tmpNum As String
	tmpNum = "683"
Dim n, i, j As Integer
Dim st As Single

'1) копирование даты начала в переменную

'2) преобразование даты начала в название мес€ца

'3)ќткрытие книги и листа с названием мес€ца

'4) копирование количества записей в столбце в переменную
	n = CInt(Cells("C3").Value) 'количество записей в столбце
' 5) активирование первой €чейки столбца
	Cells(1,13).Activate
' 6) последовательный перебор €чеек и сравнение данных €чейки (номера экспертизы) с данными переменной
For i=0  To n-1
	 	If ActiveCell.Value = tmpNum Then
			ActiveCell.Offset().Activate
			ActiveCell.Value = cof ' запись значение коэффициента нагрузки в €чейку
			ActiveCell.Offset().Activate
			ActiveCell.Value = % ' запись значение процента нагрузки в €чейку
	 	End If 
	ActiveCell.Offset().Activate
	Next i
' 7) сохранение листа и книги

' 8) выход из программы









